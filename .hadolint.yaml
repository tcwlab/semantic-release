# hadolint configuration for tcwlab/semantic-release
#
# DL3018: Pin versions in apk add
# Alpine images deliberately do not pin apk package versions, as they use
# the Alpine repository as the source of truth and only update via apk upgrade.
# Version pinning would break across Alpine releases.
ignore:
  - DL3018
